{
  "title": "Real `maximum` on integer schema",
  "description": "If an instance is guaranteed to be an integer, setting a real number upper bound is the same as flooring that bound.",
  "examples": [
    {
      "after": {
        "type": "integer",
        "maximum": 7
      },
      "before": {
        "type": "integer",
        "maximum": 7.8
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "dialects": {
    "2019-09": [ "maximum", "type" ],
    "2020-12": [ "maximum", "type" ],
    "draft4": [ "maximum", "type" ],
    "draft6": [ "maximum", "type" ],
    "draft7": [ "maximum", "type" ]
  },
  "message": "Floor the real `maximum` to an integer value.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/numeric.html#range"
  ]
}
