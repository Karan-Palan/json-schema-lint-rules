{
  "title": "`if: S, else:false` collapses to just `S`",
  "description": "`if: S, else:false` is logically equivalent to just `S` – the wrapper adds no constraint.",
  "examples": [
    {
      "after": {
        "properties": {
          "flag": {
            "const": true
          }
        }
      },
      "before": {
        "if": {
          "properties": {
            "flag": {
              "const": true
            }
          }
        },
        "else": false
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "dialects": {
    "2019-09": [ "if", "then", "else" ],
    "2020-12": [ "if", "then", "else" ],
    "draft7": [ "if", "then", "else" ]
  },
  "message": "Inline the `if` schema and drop `else:false`.",
  "references": [
    "https://json-schema.org/draft/2020-12/json-schema-core.html#name-conditionals",
    "https://json-schema.org/draft/2020-12/json-schema-core.html#name-boolean-schemas"
  ]
}
