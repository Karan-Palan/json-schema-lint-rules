{
  "title": "`else` is an empty schema",
  "description": "`else` set to the empty schema (`{}`) does not restrict validation and is most likely ineffective.",
  "examples": [
    {
      "after": {
        "if": {
          "properties": {
            "flag": {
              "const": true
            }
          }
        }
      },
      "before": {
        "if": {
          "properties": {
            "flag": {
              "const": true
            }
          }
        },
        "else": {}
      },
      "doc": true
    }
  ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: else is an empty schema",
      "description": "Empty else adds no constraint; remove or add restrictions.",
      "before": { "if": { "properties": { "flag": { "const": true } } }, "else": {} },
      "after": { "if": { "properties": { "flag": { "const": true } } } }
    },
    {
      "title": "Right: else with actual constraints",
      "description": "Provide meaningful constraints under else.",
      "before": { "if": { "properties": { "flag": { "const": true } } }, "else": { "required": ["flag"] } },
      "after": { "if": { "properties": { "flag": { "const": true } } }, "else": { "required": ["flag"] } }
    }
  ],
  "autofixable": true,
  "categories": [ "correctness" ],
  "dialects": {
    "2019-09": [ "if", "then", "else" ],
    "2020-12": [ "if", "then", "else" ],
    "draft7": [ "if", "then", "else" ]
  },
  "message": "Remove empty `else` or add restrictions.",
  "references": [
    "https://json-schema.org/draft/2020-12/json-schema-core.html#name-conditionals"
  ]
}
