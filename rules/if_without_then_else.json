{
  "title": "`if` without `then` or `else` is ineffective",
  "description": "A lone `if` keyword has no impact on validation; at least one of `then` or `else` must accompany it.",
  "examples": [
    {
      "after": {
        "if": {
          "properties": {
            "flag": {
              "const": true
            }
          }
        },
        "then": {
          "required": [ "flag" ]
        }
      },
      "before": {
        "if": {
          "properties": {
            "flag": {
              "const": true
            }
          }
        }
      },
      "doc": true
    }
  ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: if without then/else",
      "description": "`if` alone does nothing. Add `then` and/or `else` to make it effective.",
      "before": {
        "if": { "properties": { "flag": { "const": true } } }
      },
      "after": {
        "if": { "properties": { "flag": { "const": true } } },
        "then": { "required": [ "flag" ] }
      }
    },
    {
      "title": "Right: if with else only",
      "description": "Using `else` alone is also valid; triggers when `if` fails.",
      "before": {
        "if": { "properties": { "type": { "const": "A" } } },
        "else": { "not": { "required": [ "deprecated" ] } }
      },
      "after": {
        "if": { "properties": { "type": { "const": "A" } } },
        "else": { "not": { "required": [ "deprecated" ] } }
      }
    }
  ],
  "autofixable": true,
  "categories": [ "correctness" ],
  "dialects": {
    "2019-09": [ "if", "then", "else" ],
    "2020-12": [ "if", "then", "else" ],
    "draft7": [ "if", "then", "else" ]
  },
  "message": "Add `then`, `else`, or remove `if`.",
  "references": [
    "https://json-schema.org/draft/2020-12/json-schema-core.html#name-conditionals"
  ]
}
