{
  "title": "Remove empty `patternProperties` object",
  "description": "`patternProperties:{}` does nothing and should be removed.",
  "examples": [
    {
      "after": {
        "type": "object"
      },
      "before": {
        "type": "object",
        "patternProperties": {}
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: Empty patternProperties",
      "description": "An empty object under patternProperties has no effect and should be dropped.",
      "after": {
        "type": "object"
      },
      "before": {
        "type": "object",
        "patternProperties": {}
      }
    },
    {
      "title": "Right: Meaningful regex-based constraints",
      "description": "Using regex keys to constrain property names is valid.",
      "after": {
        "type": "object",
        "patternProperties": {
          "^S_": {
            "type": "string"
          }
        }
      },
      "before": {
        "type": "object",
        "patternProperties": {
          "^S_": {
            "type": "string"
          }
        }
      }
    }
  ],
  "dialects": {
    "2019-09": [ "patternProperties" ],
    "2020-12": [ "patternProperties" ],
    "draft4": [ "patternProperties" ],
    "draft6": [ "patternProperties" ],
    "draft7": [ "patternProperties" ]
  },
  "message": "Delete the empty `patternProperties` object.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/object.html#pattern-properties"
  ]
}
