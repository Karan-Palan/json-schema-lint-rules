{
  "title": "Disallow duplicate values in enum",
  "description": "Setting duplicate values in `enum` is an anti-pattern and adds no information.",
  "examples": [
    {
      "after": {
        "enum": [ "a", "b" ]
      },
      "before": {
        "enum": [ "a", "b", "a" ]
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "correctness" ],
  "dialects": {
    "2019-09": [ "enum" ],
    "2020-12": [ "enum" ],
    "draft4": [ "enum" ],
    "draft6": [ "enum" ],
    "draft7": [ "enum" ]
  },
  "message": "Remove duplicate values from the `enum` array.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/generic.html#enumerated-values"
  ]
}
