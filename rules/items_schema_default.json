{
  "title": "Omit `items:{}` (it’s the default)",
  "description": "When `items` is the always-true schema (`{}` or `true`) it adds no restriction and should be omitted.",
  "examples": [
    {
      "after": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "array"
      },
      "before": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "array",
        "items": {}
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: items:{} on array",
      "description": "An always-true items schema is the default and should be omitted.",
      "after": {
        "type": "array"
      },
      "before": {
        "type": "array",
        "items": {}
      }
    },
    {
      "title": "Wrong: items:true on array",
      "description": "Boolean true is equivalent to {}; omit it for brevity.",
      "after": {
        "type": "array"
      },
      "before": {
        "type": "array",
        "items": true
      }
    },
    {
      "title": "Right: Constraining items is meaningful",
      "description": "Specifying a schema inside items restricts element types.",
      "after": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "before": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    }
  ],
  "dialects": {
    "2019-09": [ "items" ],
    "2020-12": [ "items" ],
    "draft4": [ "items" ],
    "draft6": [ "items" ],
    "draft7": [ "items" ]
  },
  "message": "Remove `items` when it is the always-true schema.",
  "references": [
    "https://json-schema.org/draft/2020-12/json-schema-core.html#name-boolean-schemas"
  ]
}
