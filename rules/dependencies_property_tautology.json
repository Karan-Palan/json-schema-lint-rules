{
  "title": "Property already required – `dependencies` tautology",
  "description": "Using `dependencies` to declare that a property depends on itself (when it is already in `required`) is unnecessary and needlessly complex.",
  "examples": [
    {
      "after": {
        "type": "object",
        "required": [ "id", "name" ]
      },
      "before": {
        "type": "object",
        "required": [ "id", "name" ],
        "dependencies": {
          "id": [ "name" ]
        }
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "dialects": {
    "draft4": [ "dependencies", "required" ],
    "draft6": [ "dependencies", "required" ],
    "draft7": [ "dependencies", "required" ]
  },
  "message": "Remove tautological entries from `dependencies` (property is already required).",
  "references": [
    "https://json-schema.org/draft-07/json-schema-validation.html#dependencies"
  ]
}
