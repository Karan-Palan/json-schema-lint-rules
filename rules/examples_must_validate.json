{
  "title": "Values in `examples` must validate against the schema",
  "description": "Each entry in an `examples` array should be a valid instance of the schema it annotates; misleading examples hinder tooling and docs.",
  "examples": [
    {
      "after": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "examples": [ 42 ],
        "type": "integer"
      },
      "before": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "examples": [ "not-an-integer" ],
        "type": "integer"
      },
      "doc": true
    },
    {
      "after": {
        "examples": [ "user@example.com" ],
        "type": "string",
        "format": "email"
      },
      "before": {
        "examples": [ "user@example.com" ],
        "type": "string",
        "format": "email"
      }
    }
  ],
  "autofixable": false,
  "categories": [ "correctness", "readability" ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: Example contradicts type",
      "description": "All examples must be valid instances of the schema.",
      "after": {
        "examples": [ 42 ],
        "type": "integer"
      },
      "before": {
        "examples": [ "not-an-integer" ],
        "type": "integer"
      }
    },
    {
      "title": "Wrong: Example violates format",
      "description": "When using format, ensure examples conform to it.",
      "after": {
        "examples": [ "user@example.com" ],
        "type": "string",
        "format": "email"
      },
      "before": {
        "examples": [ "invalid" ],
        "type": "string",
        "format": "email"
      }
    },
    {
      "title": "Right: Multiple valid examples",
      "description": "Providing several valid examples is acceptable and useful.",
      "after": {
        "examples": [ "a", "b" ],
        "type": "string"
      },
      "before": {
        "examples": [ "a", "b" ],
        "type": "string"
      }
    }
  ],
  "dialects": {
    "2019-09": [ "examples" ],
    "2020-12": [ "examples" ],
    "draft6": [ "examples" ],
    "draft7": [ "examples" ]
  },
  "message": "Remove or fix invalid example values.",
  "references": [
    "https://json-schema.org/draft/2020-12/json-schema-validation.html#name-examples"
  ]
}
