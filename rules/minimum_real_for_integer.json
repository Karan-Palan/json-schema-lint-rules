{
  "title": 	"Real `minimum` on integer schema",
  "description": "If an instance is guaranteed to be an integer, setting a real number lower bound is the same as ceiling that bound.",
  "examples": [
    {
      "after": {
        "type": "integer",
        "minimum": 4
      },
      "before": {
        "type": "integer",
        "minimum": 3.2
      },
      "doc": true
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "dialects": {
    "2019-09": [ "minimum", "type" ],
    "2020-12": [ "minimum", "type" ],
    "draft4": [ "minimum", "type" ],
    "draft6": [ "minimum", "type" ],
    "draft7": [ "minimum", "type" ]
  },
  "message": "Ceil the real `minimum` to an integer value.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/numeric.html#range"
  ]
}
