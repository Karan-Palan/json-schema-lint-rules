{
  "title": "Round up real `minimum` for integer schemas",
  "description": "On an integer schema, a non-integer `minimum` can be replaced by its ceiling value.",
  "examples": [
    {
      "after": {
        "type": "integer",
        "minimum": 4
      },
      "before": {
        "type": "integer",
        "minimum": 3.2
      },
      "doc": true
    }
  ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: Non-integer minimum for integer type",
      "description": "Use ceiling for minimum on integer schemas.",
      "before": { "type": "integer", "minimum": 3.2 },
      "after": { "type": "integer", "minimum": 4 }
    },
    {
      "title": "Right: Integer minimum",
      "description": "An integer-typed schema with an integer minimum is correct.",
      "before": { "type": "integer", "minimum": 0 },
      "after": { "type": "integer", "minimum": 0 }
    }
  ],
  "autofixable": true,
  "categories": [ "style" ],
  "dialects": {
    "2019-09": [ "minimum", "type" ],
    "2020-12": [ "minimum", "type" ],
    "draft4": [ "minimum", "type" ],
    "draft6": [ "minimum", "type" ],
    "draft7": [ "minimum", "type" ]
  },
  "message": "Ceil the real `minimum` or convert it to an integer value.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/numeric.html#range"
  ]
}
