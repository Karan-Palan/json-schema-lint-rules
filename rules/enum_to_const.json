{
  "title": "Replace single-value enum with const",
  "description": "An enum with exactly one element can be expressed more clearly with const.",
  "examples": [
    {
      "after": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "const": "foo"
      },
      "before": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "enum": [ "foo" ]
      },
      "doc": true,
      "tags": [ "basic" ]
    },
    {
      "before": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "enum": [ 42 ]
      }
    }
  ],
  "comprehensiveExamples": [
    {
      "title": "Wrong: Single-value enum",
      "description": "Replace enum with a single element by const for clarity.",
      "before": { "enum": ["foo"] },
      "after": { "const": "foo" }
    },
    {
      "title": "Right: Multi-value enum remains enum",
      "description": "Enums with multiple values are correct and should remain enums.",
      "before": { "enum": ["foo", "bar"] },
      "after": { "enum": ["foo", "bar"] }
    }
  ],
  "autofixable": true,
  "categories": [ "correctness", "readability" ],
  "dialects": {
    "2019-09": [ "enum", "const" ],
    "2020-12": [ "enum", "const" ]
  },
  "message": "Single-value enum detected – replace with const.",
  "references": [
    "https://json-schema.org/understanding-json-schema/reference/const"
  ]
}
